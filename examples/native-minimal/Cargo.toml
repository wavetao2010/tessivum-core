[package]
name = "tessivum-native-minimal"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde_json = "1"
tessivum-core = { path = "../../crates/tessivum-core" }
tokio = { version = "1", features = ["macros", "rt"] }
